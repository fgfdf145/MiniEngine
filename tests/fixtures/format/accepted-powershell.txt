# if ($true) { comment content must not count
$single = 'catch {} and process {}'
$double = "if (`$true) { and begin {}"
$switchText = '"literal" { return }'
# default { return "comment" }
$here = @"
	function Hidden-In-HereString {
	catch {}
	default { return "here-string" }
"@
$literal = @'
	process {
'@
<#
dynamicparam {
class Hidden {
#>
<#
<# nested comment #>
function Still-Hidden {
#>
$hash = @{
    key = "value"
}
ForEach-Object { $_ }
function Invoke-GoodThing
{
    begin
    {
        "ok"
    }
    process
    {
        "ok"
    }
    end
    {
        "ok"
    }
}
